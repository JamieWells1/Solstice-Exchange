#ifndef TRUNCATE_H
#define TRUNCATE_H

#include <string>

namespace solstice::matching
{

std::string truncate(std::string str, size_t length);

}  // namespace solstice::matching

#endif  // TRUNCATE_H
